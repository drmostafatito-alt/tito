# Test Plan

> Status: **Phase 0 strategy.** Per-phase execution results are appended in §6.
> Rule: a phase is not done until its rows in §4 pass and the regression checklist (PROJECT-PLAN §6) is green.

## 1. Layers & tools

| Layer | Tool | Runs | What it proves |
|---|---|---|---|
| Unit | Vitest | every PR | entitlement resolver matrix, exam timing/deadline math, grading & partial credit, code hashing/normalization, policy evaluators, settings Zod schemas |
| Integration | Vitest + Workers runtime (miniflare pool) with real local D1/R2 | every PR | auth flows, device policy, file signing, webhook signature verification + idempotency, transactional grant paths, attempt finalization under races |
| E2E | Playwright (local `wrangler dev` target) | pre-release + per-phase for critical flows | student journey end-to-end, exam journey, admin content → student visibility |
| Device matrix | Manual, real devices | Phases 3, 7 (+touch points per phase) | iPhone Safari/Chrome, iPad Safari, Android Chrome, desktop Chrome/Edge |

## 2. Critical-flow e2e scripts (owned by phases)

- P1: register → login → logout → reset password → wrong-password lockout; admin guard (student hitting `/admin` gets 403 server-side).
- P2: admin creates course tree → publishes → student sees/denies per access_level; private file URL without signature 404s; signed URL expires.
- P3: entitled student plays video (mock+real), resume across devices, replay cap blocks N+1 watch; unentitled token mint → 403.
- P4: timed exam with refresh mid-way → answers recovered → submit idempotent (double-click, network retry) → deadline expiry auto-submits per policy → essay manual grading → results visibility per policy.
- P5: manual checkout → admin approval → entitlement appears; activation code redeem once (two concurrent tabs → exactly one success); discount math; refund revokes/shortens entitlement per policy.
- P3 ✅: CMS edit page → publish → public page reflects without deploy; draft never public; preview admin-only; audit rows for every CMS mutation; unsafe HTML/links sanitized or rejected (unit + integration + runtime smoke §12).
- P7: full regression + performance budgets + device matrix.

## 3. Authorization test matrix (unit, exhaustive — the crown jewels)

For each content type × each state: {public, authenticated, entitled-free, entitled-paid, expired entitlement, revoked, unpublished, scheduled-future, expired-content, free_preview flag, admin_override} × {anon, student, teacher, admin} → expected verdict. Any new access source extends this matrix first (test-first rule for the resolver).

## 4. Coverage matrix (brief §46 mapped)

| Area | Layer(s) | Phase |
|---|---|---|
| Auth, RBAC, course access, free/paid content | unit+integration+e2e | 1–3 |
| Subscriptions, activation codes, payments, webhooks | integration (signed fixtures) | 5 |
| Video playback/protection/replay limits | integration + device | 2–3 |
| Device restrictions, sharing deterrence | integration | 1,3 |
| PDF permissions (view/download/expiry) | integration | 2 |
| Exam timing, submission, attempts, randomization, essay grading | unit+integration+e2e | 4 |
| Progress tracking | integration | 3 |
| Admin actions + audit | e2e | 6,7 |
| Admin platform: real-metrics dashboard, user mgmt + escalation guards, announcements lifecycle/visibility, analytics aggregation (no double-count), security/audit viewers, RBAC/IDOR | unit+integration+e2e | 7 |
| Mobile responsiveness / iPhone Safari / Android / desktop | device matrix | 3,7 |

## 5. Device matrix & iOS-specific checklist (manual, real hardware — owner-assisted where needed)

- iPhone Safari: safe-area insets, `dvh` behavior, fixed bottom nav, sticky header, keyboard push (form fields, modal scroll), video playsinline/fullscreen/rotation/PiP, backgrounding mid-exam and mid-video (resume), network loss + airplane-mode toggle during exam, session persistence.
- iPhone Chrome (WebKit): same core checks.
- iPad Safari: layout breakpoints, landscape.
- Android Chrome: video, keyboard, 100vh.
- Desktop Chrome/Edge: full admin flows, uploads.
- Honest boundary: this sandbox cannot run real iPhones; the build encodes the iOS rules (ARCHITECTURE §15) and automated layout tests cover what's simulatable; final sign-off is on hardware.

## 6. Execution log

| Phase | Date | Scope | Result |
|---|---|---|---|
| 1 | 2026-09-05 | Static: check-imports, typegen (17 files), tsc, unit (32/32), integration (7/7), production build (client+SSR) | ✅ all green |
| 1 | 2026-09-05 | Runtime (`wrangler dev`, production-path worker, local D1 + `.dev.vars`): home 200 `lang=ar dir=rtl` rendered from D1 settings; 5 security headers + strict CSP; static asset 200; cross-site POST blocked (400/403), same-origin passes; unauth `/dashboard` → 302 login; admin + student login 302 + `__edu_session` (`HttpOnly; Secure; SameSite=Lax`); session → D1 lookup (dashboards 200); `/admin` RBAC: admin 200, student → 404-style redirect + 4 `permission_denied` events in D1; device-concurrency limit enforced (`device_limit`); logout 302 → post-logout redirect; workerd RSS ~150 MB, no OOM through ~25 requests | ✅ all green |
| 2 | 2026-09-05 | Static: check-imports (+ new unused-server-import guard), tsc 0 errors, unit **52/52**, integration **37/37** — hermetic (passes with AND without `.dev.vars`; miniflare supplies test-only secret bindings), production build client+SSR green; `npm run verify` exit 0 | ✅ all green |
| 2 | 2026-09-05 | DB integrity: migrations 0000+0001 applied to local D1; seed run twice → idempotent (stable row counts); SQL spot checks: lesson_item→videos, lesson_item→files, entitlement→subjects all resolve; content-domain FK deferral covered by app-layer `ContentReferenceError` guards (ADR-017) incl. live HTTP test (dangling video attach rejected) and regression tests | ✅ all green |
| 2 | 2026-09-05 | Runtime HTTP smoke (`scripts/smoke.mjs` vs live `wrangler dev`, seeded local D1+R2) — **104/104**: headers+strict CSP on public/lesson/file responses; anon denial (lesson → 302 login?next, playback → 401); CSRF both layers (evil Origin → 400 by RR7 `throwIfPotentialCSRFAttack`; `sec-fetch-site: cross-site` → 403 app middleware); GET /logout → 405; student/admin login + HttpOnly session cookies; RBAC student→/admin 404-shaped; free_preview vs entitled lesson gating; signed file URLs: view=inline/download=attachment, unsigned/tampered-sig/wrong-id/perm-swap/expired → 404, Range → 206, no-store; mock playback: provider-neutral JSON, ≤45s TTL, master.m3u8 200, forged/missing token → 404, poster thumbnail-scope 200 (regression), GET playback → 302, unknown id → 404; unentitled new student: locked page w/o signed URLs, protected playback → 403; admin CRUD over HTTP: create program/grades, slug generation, move-up ordering + boundary no-op, rename, draft, archive (catalog unaffected); upload → listed → signed fetch 200 %PDF; video register → attach to lesson → dangling-ref attach rejected; entitlement grant → access flips server-side (signed URLs + playback 200); logout revokes (dashboard 302, playback 401); rate limit blocks ≤10/min and refuses VALID credentials while limited | ✅ 104/104 |
| 2 | 2026-09-05 | Provider-switch matrix (live): settings `video.provider` → `mux` with absent API credentials → ingest fails loudly (`VideoNotConfiguredError: missing MUX_TOKEN_ID, MUX_TOKEN_SECRET`), row stored as provider=mux status=pending (not playable), NO silent mock fallback; revert → mock ingest succeeds (status=ready); test rows cleaned | ✅ all green |
| 3 | 2026-09-05 | Static: check-imports ✓ · tsc 0 errors · unit **61/61** (9 new CMS registry tests) · integration **46/46** (9 new CMS tests: sanitizer, publish/restore lifecycle, unsafe-link rejection, render resolvers + presentation toggles, public-only image resolution, form validation, menu href safety) · production build ✓ · `npm run verify` exit 0 | ✅ all green |
| 3 | 2026-09-05 | Runtime HTTP smoke vs live `wrangler dev` (cold seeded local D1+R2) — **126/126**: all Phase 1–2 regressions (auth/RBAC/devices/signed-URLs/playback/grants/revocation/rate-limits) + §12 CMS: theme.css tokens, favicon 204 empty-first, page create → draft-404 → section+blocks → publish → anonymous render, preview admin-only (anon → login redirect), header menu → visible on /, form create → embed → invalid submit shows failure message in place, valid submit shows success, system tab: platform rename + provider policy saved → new name visible to anonymous (zero-deploy branding) | ✅ 126/126 |
| 3 | 2026-09-05 | Production-readiness gate both directions: seeded dev DB → 6 FAIL exit 1 (demo accounts, seed/smoke content, mock provider, placeholder media, template branding, empty owner identity); isolated fresh DB (migrations + `bootstrap-admin.mjs` + owner-config settings only) → **10/10 PASS exit 0** — empty-first production state proven achievable (ADR-020) | ✅ verified |
| 3 | 2026-09-05 | Security/access audit (stage 9): granular `cms.*` permission checks on every admin CMS route (loader+action); public renders published snapshots only; drafts 404; preview admin-gated; sanitizer/safeHref/icon-registry/theme-token validation unit+integration tested; no inline styles (CSP); every content/config mutation audited (bootstrap seeders + visitor submissions excepted by design); form submits rate-limited, declarative-only, no file writes; `resolvePublicImageUrls` PUBLIC_ASSETS-only | ✅ clean |
| 3 | 2026-09-05 | Mobile/responsive audit (stage 7, static + build review — no real device in sandbox): viewport-fit=cover, pt/pb-safe utilities, dvh (no 100vh), mobile-first grid presets for all 38 blocks + section columns, safe-area-aware floating CTAs, admin header mobile nav toggle added (44px targets), builder tool buttons 44px on small screens; real-device matrix remains owner-assisted per §5 | ✅ code-level green |
| 2 | 2026-09-05 | Security audit: secret VALUES absent from `build/client` + `build/server` bundles (per-var grep); secret NAMES + `.server` module code absent from client bundle; `.dev.vars` gitignored (`.dev.vars.*` too, example allowed); private R2 bucket only reachable via signed `/files/:id` (guessed object paths 404); `build/` added to `.gitignore` | ✅ all green |
| 4 | 2026-09-05 | Static: check-imports ✓ · tsc 0 errors · unit **63/63** (+2: video progress knobs defaults/bounds, dashboard module ids) · integration **58/58** (+12 `progress.test.ts`: beacon auth 401 / entitlement 403 / unknown refs 404 / heartbeat upsert / threshold completion with EXACTLY-ONE `lesson_complete` event / mint watch accounting / resumeAt / replay-limit 403 + admin bypass / course % / continue-learning / stats) · production build ✓ · `npm run verify` exit 0 | ✅ all green |
| 4 | 2026-09-05 | DB: additive migration `0004_amused_hiroim` (4 progress tables + 7 indexes, pure DDL) applied to local D1 and verified on an ISOLATED fresh DB (`--persist-to /tmp`, all 5 migrations ✅); integration suite re-applies all migrations to a cold DB every run; no renames/drops/destructive statements | ✅ all green |
| 4 | 2026-09-05 | Runtime HTTP smoke vs live `wrangler dev` (cold seeded local D1+R2, fresh jar dir) — **162/162**: all Phase 1–3 regressions (126) + §14 journey (36): catalog hierarchy pages (programs/subjects 200, unknown slugs 404, published-only links), course/unit pages per-lesson locks (anon sees NO learn links on entitled course; login invite), beacon gates (anon 401, GET 405, unentitled+lesson context 403), heartbeat → re-mint `resumeAt=30`, server-decided threshold (107/120 not completed, 115 ended → video+lesson auto-completed, badge renders), completed video → `resumeAt=0`, manual mark-complete (200 + state, unknown action 400, unentitled 403), dashboard real-data modules (continue + stats + 100% course progress), profile (email shown, save + alert, invalid input safe, name propagates to shell), student mobile nav present | ✅ 162/162 |
| 4 | 2026-09-05 | Security audit (progress/playback): beacons re-check session + lesson entitlement server-side every request; positions clamped, self-report never authorizes; completion decided server-side (settings threshold); replay limit enforced PRE-mint (denied replay never yields credentials); `startWatch` wrapped so progress failure cannot deny entitled playback; toggle-complete action re-checks entitlement (403 verified live); profile action validates via zod (fail-closed) + logs `profile_updated` security event; locale cookie sync only on preference change; CMS student menu hrefs pass `safeHref` (existing menu validation) | ✅ clean |
| 4 | 2026-09-05 | Mobile/responsive audit (code-level, no real device in sandbox): student header mobile nav toggle (44px target, aria-expanded/controls), nav links min-h-11, `min-h-dvh` shell + `pt-safe`/`pb-safe`, profile form 44px inputs, dashboard grids collapse to 1 column, ProgressBar is class-only (CSP `style-src 'self'` safe), player resume/notice bars inside existing responsive layout; real-device matrix remains owner-assisted per §5 | ✅ code-level green |
| 5 | 2026-09-05 | Static: check-imports ✓ · tsc 0 errors · unit **76/76** (+13: exam-config contract defaults/merges/rejections, seededShuffle determinism/purity/permutation, choiceSeed stability) · integration **96/96** (+38 `assessment.test.ts`: bank CRUD/shape-validation/type-immutability/status-flow/duplicate/delete-guard/tags · publish gate (no questions / draft-only attach / essay refused / pool resolution) · config merge · access (anon/non-entitled/entitled) + route gates (302 login / 403 / 404 unpublished) · windows + caps + cooldown · start materialization + duplicate-start resume + exactly-once exam_start · sanitized live context (key-set assertions) · autosave upsert/version/retry + route refresh-resume · IDOR (entitled B → 404 on A's result/save; random ids 404) · timing (deadline = start+duration, sweep auto-submit with answered-so-far, late submit clamped at deadline+grace, route touch-sweep → `?expired=1`) · submit idempotency (double submit = stored result, ONE exam_submit) + post-submit lock (`closed`) · grading (full/zero/unanswered/partial 1.5-of-3/boundary 50%/points-override frozen) · results policy (immediate/manual→flip/after_end) + review gating (review_mode/show_answers/show_explanations) · progress (start ≠ completion; graded → lesson completed)) · production build ✓ · `npm run verify` exit 0 | ✅ all green |
| 5 | 2026-09-05 | DB: additive migration `0005_illegal_mother_askani` (8 assessment tables + 9 indexes incl. partial-unique one-live-attempt + `assessment.*` permission seed) applied to local D1 and verified on an ISOLATED fresh DB (`--persist-to /tmp`, all 6 migrations ✅, 21 statements); no renames/drops/destructive statements; drizzle 0.45 partial-index support confirmed | ✅ all green |
| 5 | 2026-09-05 | Runtime HTTP smoke vs live `wrangler dev` (cold seeded local D1+R2, fresh jar dir) — **193/193**: all Phase 1–4 regressions (162) + §15 journey (31): anon /exams → login redirect; unentitled → 403 on intro AND attempt routes; student → /admin/assessment RBAC redirect; lesson page renders the real exam link; intro policy (start button + duration); start → 302 attempt; attempt hooks + server countdown + sanitized payload (no isCorrect/explanation/feedback) + seeded stem; autosave v1 → duplicate retry v2 (one row) → refresh restores saved answer; admin grant → entitled student B: A's result 404 + save-on-A's-attempt 404 JSON; unknown attempt 404; submit → JSON redirect /results/:id; DOUBLE submit → same stored result; results 2/3 + 66.7% + ناجح + review section; save-after-submit `{error:'closed'}`; attempt page → 302 intro (not reusable); history shows محاولة 1 + مُصححة; attempt 2 = new id (consumed to keep re-runs clean); /results index; admin hub question row + exams tab; rate-limit checks remain LAST | ✅ 193/193 |
| 5 | 2026-09-05 | Production-readiness gate both directions: seeded+smoked dev DB → 5 FAIL exit 1 (demo accounts ×3, seed/smoke content ×17 incl. demo exam/questions, mock provider, placeholder media, owner identity) with assessment scans detecting the seeded exam; isolated fresh DB (migrations only) → content+assessment scans PASS 0 (no false positives), only the 4 expected unconfigured checks fail (provider/branding/owner/no-admin) — empty-first still provable via bootstrap flow | ✅ verified |
| 5 | 2026-09-05 | Security audit (assessment): live-attempt payload sanitized at the ONLY serializer; results/review double-gated (visibility ∧ review policy); IDOR via `getOwnedAttempt(attemptId, sessionUserId)` on every attempt/results access; entitlement re-checked on EVERY exam route + mutation (loader AND action AND resource route); timing inputs are server-only (no client timestamp parsed anywhere); post-submit immutability (`closed`); admin mutations permission-gated (`assessment.*`, rank≥4 bypass) + audited (`logAudit` per mutation); students cannot reach admin routes (requireRole 3 in layout + per-route) | ✅ clean |
| 5 | 2026-09-05 | Mobile/responsive audit (code-level, no real device in sandbox): attempt page is focused-mode (no chrome), sticky header/footer, 44px choice buttons + nav targets, navigator in `<details>` (collapsed by default on small screens), confirmation dialog full-width-safe, countdown chip mono; admin builder grids collapse (sm/lg); exam cards reuse the P4 card system; class-only styling (CSP `style-src 'self'` safe) | ✅ code-level green |
| 6 | 2026-09-05 | Static: check-imports ✓ · tsc 0 errors · unit **94/94** (+18 `commerce.test.ts`: integer money math incl. the 0.1+0.2 artifact, floor discounts + clamps, PAYMENTS §2 state machines (legal/illegal jumps), promo-window boundaries, period-end math, code normalization + 300-draw uniqueness/charset, entitlement-spec/price-plan/product-kind/discount/batch input contracts) · integration **152/152** (+56 `commerce.test.ts`: products CRUD/kind-binding/ref-validation/archive · price plans (recurring-only-for-plans, promo rules) · checkout (server totals, frozen spec, instructions snapshot, dedupe/reuse, already_entitled, rail-off, cross-product plan) · route-level price+product tampering ignored · manual rail (confirm grants NOTHING, ownership, amount mismatch, approve→grant→resolver allows, double-approve idempotent, reject→retry on new row, fake-paid SQL forgery grants nothing, cancel, TTL sweep) · refunds (revoke→resolver denies, history kept, idempotent, window) · activation codes (hash-only storage, plaintext-once, valid/invalid/disabled/expired/exhausted/already_redeemed, CONCURRENT race → exactly one winner + one grant set, route redeem, batch detail leaks nothing) · discounts (percent floor + redemption rows, generic invalid (no oracle), max_uses/per-user/min-order) · mock webhooks (signed fulfill + inbox, forged → 400 + signature_valid=0, replay → duplicate, amount mismatch → flagged under_review, failed event, unknown provider 404, route 405/400) · subscriptions (purchase → active + expiring entitlement + event, already_subscribed, renew extends, cancel keeps access, pause revokes/resume restores, expiry sweep, refund cancels) · RBAC/IDOR (student → admin hub redirect, self-approve blocked, canCommerce permission rows + rank4 bypass, order IDOR 404-shaped, owner-scoped lists) · catalog (publicProductBySlug archive/promo, purchasableFor direct/subject-level, course-page CTA appears/disappears with entitlement)) · production build ✓ · `npm run verify` exit 0 | ✅ all green |
| 6 | 2026-09-05 | DB: additive migration `0006_flowery_tempest` (15 commerce tables + unique guards (order_number, provider_event_id, code_hash ×2, (code,student) redemption, product_items) + indexes + 7 `commerce.*` permission rows) applied to local D1 and verified on an ISOLATED fresh DB (`--persist-to /tmp`, all 7 entries ✅); gate on fresh DB: migrations 7/7, permissions **22/22**, commerce scans PASS 0 false positives; no renames/drops/destructive statements | ✅ all green |
| 6 | 2026-09-05 | Runtime HTTP smoke vs live `wrangler dev` (cold seeded local D1+R2, fresh jar dir) — **229/229**: all Phase 1–5 regressions (193) + §16 commerce journey (36): admin hub 200 → create product (course target from hub select) → create active plan (25000 minor = 250.00 EGP) → fresh buyer: /admin/commerce RBAC redirect → locked course page shows buy CTA with cheapest server-read price → /products/:slug 200 + server price → checkout POST with tamper fields (amountMinor=1, currency=USD) → 302 /orders/EC-… → order shows SERVER total 250.00 EGP (no 0.01/USD) + reference = order number → pending order grants NO access → confirm transfer reference → under_review → STILL no access → duplicate confirm rejected → admin queue lists order + evidence → wrong amount → amount_mismatch (nothing granted) → exact approve → saved → replayed approve → alreadyProcessed (no duplicate grants) → lesson UNLOCKED via the same resolver → CTA gone → my-orders lists paid order; failure path: fresh rejection-student starts locked → checkout → confirm → admin REJECTS → still locked → self-approve attempt → redirect + still locked; activation: batch generated (plaintext once) → valid redeem → success → lesson unlocked → replay → already_redeemed → fabricated code → invalid; webhook guards: GET → 405, forged signature → 400; rate-limit checks remain LAST | ✅ 229/229 |
| 6 | 2026-09-05 | Production-readiness gate both directions: seeded+smoked dev DB → 6 FAIL exit 1 (demo accounts ×5, seed/smoke content ×19 incl. demo/smoke products, NEW commerce scan: demoOrders=2 + demoBatches=1 + mockPayments=0, mock video provider, placeholder media, owner identity); isolated fresh DB (migrations only) → commerce scans PASS 0, only the 4 expected unconfigured checks fail — empty-first still provable | ✅ verified |
| 6 | 2026-09-05 | Security audit (commerce): price tampering ignored (server re-reads; asserted at route level in tests+smoke) · product/plan tampering validated (cross-product plan → price_unavailable) · order IDOR → 404-shaped (service returns null for non-owners; loader/action throw 404) · fake-paid (forced SQL status) yields NO entitlement — resolver reads entitlements only · webhook forgery rejected pre-parse + recorded signature_valid=0 · activation race → one winner (concurrent integration test) · paid status alone ≠ authorization · students cannot verify payments/create products/alter prices/grant entitlements (requireRole + canCommerce + route RBAC smoke) · no provider secrets in frontend (registry is server-only; mock gated by binding) · checkout/code-redeem rate-limited · every admin commerce mutation audited | ✅ clean |
| 6 | 2026-09-05 | Mobile/responsive audit (code-level, no real device in sandbox): product/checkout/orders/activate pages reuse the P4 card+form system (min-h-11 targets, single-column mobile grids, dir=ltr islands for money/codes/order numbers), admin commerce hub tabs horizontally scrollable, payment-review forms wrap (flex-wrap), code textarea full-width mono; class-only styling (CSP safe) | ✅ code-level green |
| 7 | 2026-09-05 | Static: check-imports ✓ · tsc 0 errors · unit **108/108** (+14 `tests/unit/admin-platform.test.ts`: range window math today/7d/30d/all + `parseRange` defaults, `isVisibleToRole` audience/status/window matrix, minor-units money formatting, badge/status helpers) · integration **187/187** (+35 `tests/integration/admin-platform.test.ts`: `adminOverview`/`analyticsDetail` EXACT aggregate values over seeded events/watch-sessions/attempts/orders (starts=`video_start`, completions=`video_complete`, lesson completions=`lesson_complete`, submissions=`submitted_at`, watch=`SUM(watched_seconds)`, revenue=paid-only integer minors, net=gross−refunds — page views never counted, no double-counting), date-window boundaries (+40d order lands in `all` only), `listUsers` search/filter/pagination, `userAdminDetail` aggregates + NO password-hash/token material, escalation guards (target-rank≥actor-rank rejected, self-actions rejected, super_admin grant needs rank 4, last-super-admin protected), suspend ⇒ sessions revoked + security event + audit row, announcements lifecycle (create⇒draft, publish window guard, unpublish⇒draft keeps publishedAt, archive frozen), visibility (draft/archived/expired/audience — admins match `all` only), lazy reads (`onConflictDoNothing` idempotent, `markAllRead` ≤100), `listAuditLogs` q/entityType filters + pagination, route-level loaders THROW 403 / actions return denied) · production build ✓ · `npm run verify` exit 0 | ✅ all green |
| 7 | 2026-09-05 | DB: additive migration `0007_little_aqueduct` (`announcements` + `announcement_reads` UNIQUE(announcement,user) + 4 indexes: (status,publish_at), watch_sessions(started_at), exam_attempts(started_at), orders(status,created_at) + 6 idempotent permission INSERTs — pure DDL, no drops/renames) applied to local D1 and verified on an ISOLATED fresh DB (`--persist-to /tmp`, 8/8 ✅, permissions 28/28); integration suite re-applies all migrations to a cold DB every run | ✅ all green |
| 7 | 2026-09-05 | Runtime HTTP smoke vs live `wrangler dev` (cold seeded local D1+R2, fresh jar dir) — **273/273**: all Phase 1–6 regressions (229) + §17 admin platform (44): dashboard renders real metrics from D1 (users/students/revenue-minors/watch-time/exams via `home-metric-*` extraction; range switch re-renders server-side) → users search finds §14 student → detail shows aggregates + leaks no `$2b$`/hash material → super promotes to admin → rank-3 CAN view users+analytics → rank-3 self-escalation to super_admin DENIED (and changed nothing) → self force-logout DENIED → self-suspend DENIED → super suspends → live session dies immediately (302) → list badge `موقوف` on THIS user → reactivate → badge `نشط` → revoked session STAYS revoked → demote cleanup → student IDOR on user detail denied; announcements: created as draft → draft INVISIBLE to student → publish → visible in student `/notifications` → unread badge + nav count → mark-read → badge gone → `announcements.publish` audit trail searchable → student POST to announcements admin denied; analytics/security(events+sessions)/assessment/commerce-orders/audit surfaces 200 for admin; student denied ALL 6 admin paths (302, no data). §17 consumes ZERO register/login budget (reuses §14 jar) so §13 rate-limit checks still run last with their full window | ✅ 273/273 |
| 7 | 2026-09-05 | Production-readiness gate both directions: seeded+smoked dev DB → content checks FAIL by design (demo/smoke rows incl. NEW announcements scan `Smoke %`/`إعلان Smoke%`) while structural checks PASS — migrations 8/8, **permissions 28/28** (floor raised 22→28), ≥1 active super admin; isolated fresh DB (migrations only) → ALL content/data checks PASS 0 (incl. announcements), only the 4 expected deployment-config checks fail (provider/identity/owner/bootstrap) — empty-first still provable | ✅ verified |
| 7 | 2026-09-05 | Security audit (admin platform): every P7 loader throws 403 without its read permission and every action re-checks server-side (UI hiding never authorizes) · low-privilege admin CANNOT reach super_admin (service-level rank guard, smoke+integration proven) · no password hash/session token/device key/code hash/provider secret in any admin payload (route-level assertions + smoke needle scans) · audit viewer exports NO action (append-only) · entitlement revoke routed through the EXISTING resolver service + audited · announcement drafts never leave the server for students · analytics authorization enforced (403 without `analytics.read`) · user isolation: student IDOR → 302 no data · suspend kills sessions server-side instantly | ✅ clean |
